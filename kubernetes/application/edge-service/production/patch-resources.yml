apiVersion: apps/v1
kind: Deployment
metadata:
  name: edge-service
spec:
  template:
    spec:
      containers:
        - name: edge-service
          resources:
            requests:
              cpu: "0.1"
              memory: 756Mi
            limits:
              cpu: "2"
              memory: 756Mi